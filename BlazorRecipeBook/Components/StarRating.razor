@rendermode InteractiveServer

<div>
    @for (int i = 1; i <= 5; i++)
    {
        <span class="rating-star @(i <= Rating ? "active" : "")">★</span>
    }
</div>

@code {
    [Parameter]
    public int Rating { get; set; }
}



